# 🚀 قالب صفحه اشتراک برای 3x-ui

[🇺🇸 English](README.md)

این قالب، یک صفحه اشتراک مدرن، واکنش‌گرا و زیبا برای پنل **3x-ui** است که اطلاعات کاربر را به‌صورت گرافیکی و کاربرپسند نمایش می‌دهد.

## ✨ ویژگی‌ها

* نمایش دایره‌ای حجم مصرف‌شده و باقی‌مانده
* نمایش وضعیت فعال یا غیرفعال سرویس
* نمایش تاریخ انقضا و آخرین اتصال
* لیست کانفیگ‌ها با قابلیت کپی
* لینک اشتراک با دکمه کپی
* پشتیبانی از زبان فارسی و انگلیسی
* تم روشن و تاریک
* پشتیبانی از فایل `config.json` برای تنظیم آیدی پشتیبانی

---

## 📁 ساختار پروژه

```text
3x-ui-Subscription-Template/
├── sub.html           (فایل اصلی قالب - نام باید sub.html باشد)
├── config.json        (فایل تنظیمات)
├── install.sh         (اسکریپت نصب تک‌خطی)
├── README.md          (مستندات انگلیسی)
└── README_FA.md       (مستندات فارسی)
```

---


## 🛠️ نصب و راه‌اندازی

### ۱. اجرای اسکریپت نصب

کافی است این دستور را (با کاربر root) روی سرور اجرا کنید:

```bash
bash <(curl -Ls https://raw.githubusercontent.com/mahdizo3181/3x-ui-Subscription-Template/main/install.sh)
```

اسکریپت قالب را دانلود می‌کند، آیدی پشتیبانی را می‌پرسد (اختیاری)
و در پایان مسیری را که باید در پنل وارد کنید نمایش می‌دهد:

```text
  Installation complete.

  Put this path in your 3x-ui panel:

      /opt/3x-ui-sub-template
```

برای به‌روزرسانی قالب، همین دستور را دوباره اجرا کنید؛ فایل `config.json` شما حفظ می‌شود.

برای نصب در مسیر دلخواه:

```bash
INSTALL_DIR=/root/my-template bash <(curl -Ls https://raw.githubusercontent.com/mahdizo3181/3x-ui-Subscription-Template/main/install.sh)
```

---

### ۲. تنظیم مسیر قالب در 3x-ui

1. وارد پنل 3x-ui شوید.
2. به بخش **تنظیمات پنل** بروید.
3. **سابسکریپشن** را باز کنید.
4. روی **پروفایل** کلیک کنید.
5. مسیری که اسکریپت نمایش داده را در قسمت **پوشه قالب صفحه اشتراک** وارد کنید:

```text
/opt/3x-ui-sub-template
```

6. تغییرات را ذخیره کنید.
7. یک‌بار پنل را ریستارت کنید:

```bash
x-ui restart
```

> فایل قالب باید با نام `sub.html` ذخیره شده باشد.

---

### ۳. تغییر آیدی پشتیبانی (اختیاری)

فایل `config.json` داخل پوشه نصب را ویرایش کنید:

```bash
nano /opt/3x-ui-sub-template/config.json
```

```json
{
  "support_id": "@your_support_username"
}
```

در صورت خالی بودن این مقدار، لینک پشتیبانی نمایش داده نخواهد شد. نیازی به ریستارت پنل نیست.

---

### ۴. تست نهایی

یک کاربر فعال ایجاد کنید و لینک اشتراک او را باز کنید. قالب جدید به‌صورت خودکار نمایش داده خواهد شد.

---

## 🔧 نکات مهم

* نام فایل قالب حتماً باید `sub.html` باشد.
* از مسیر مطلق (Absolute Path) استفاده کنید.
* مطمئن شوید فایل `sub.html` داخل پوشه انتخاب‌شده وجود دارد.
* برای اعمال تغییرات `config.json` نیازی به ریستارت پنل نیست.
* نصب دستی هم همچنان کار می‌کند: مخزن را clone کنید و مسیر همان پوشه را وارد پنل کنید.

---

## 🧑‍💻 شخصی‌سازی

* برای تغییر رنگ‌ها، بخش `:root` در CSS را ویرایش کنید.
* برای تغییر متن‌ها، آبجکت `i18n` در جاوااسکریپت را ویرایش کنید.

---

## 📄 مجوز

استفاده، ویرایش و توسعه این قالب برای پروژه‌های شخصی و عمومی آزاد است.

---

**توسعه‌دهنده:** مهدی ظهرابی

**گیت‌هاب:** https://github.com/mahdizo3181/3x-ui-Subscription-Template
